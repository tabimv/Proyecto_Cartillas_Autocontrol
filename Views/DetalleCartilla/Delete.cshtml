@model Proyecto_Cartilla_Autocontrol.Models.DETALLE_CARTILLA

@{
    ViewBag.Title = "Delete";
}

@if (Session["Perfil"] != null && Session["Perfil"].ToString() == "Administrador")
{
    Layout = "~/Views/Shared/_Layout.cshtml";
}
else
{
    Layout = "~/Views/Shared/_LayoutGeneral.cshtml";
}

@if (Session["Perfil"] != null && Session["Perfil"].ToString() == "Administrador")
{
    <link rel="stylesheet" href="~/Content/css/delete.css">

    <div class="container-delete">
        <div class="form-container-delete">
            <h2> Eliminar Detalle Cartilla </h2>
            <h3>¿Estás seguro que quieres eliminar este Detalle de Cartilla?</h3>
            <div>
                <hr />
                <dl class="dl-horizontal">
                    <dt>
                        Estado OTEC:
                    </dt>

                    <dd>
                        @Html.DisplayFor(model => model.estado_otec)
                    </dd>

                    <dt>
                        Estado ITO:
                    </dt>

                    <dd>
                        @Html.DisplayFor(model => model.estado_ito)
                    </dd>

                    <dt>
                        Código Actividad:
                    </dt>

                    <dd>
                        @Html.DisplayFor(model => model.ACTIVIDAD.codigo_actividad)
                    </dd>
                    <dt>
                        URL Documento:
                    </dt>

                    <dd>
                        @Html.DisplayFor(model => model.CARTILLA.ruta_documento_pdf)
                    </dd>
                    <dt>
                        Tipo Inmueble
                    </dt>

                    <dd>
                        @Html.DisplayFor(model => model.INMUEBLE.tipo_inmueble)
                    </dd>
                    <dt>
                        Elemento Verificación:
                    </dt>

                    <dd>
                        @Html.DisplayFor(model => model.ITEM_VERIF.elemento_verificacion)
                    </dd>

                </dl>


                @if (!string.IsNullOrEmpty(ViewBag.ErrorMessage))
                {
                    <div class="alert alert-danger">@ViewBag.ErrorMessage</div>
                }

                @using (Html.BeginForm())
                {
                    @Html.AntiForgeryToken()

                    <div class="form-actions no-color">
                        @Html.ActionLink("Volver a la Lista", "Index", null, new { @class = "btn btn-sm btn-custom-link" }) |
                        <input type="submit" value="Eliminar" class="btn btn-sm btn-primary-custom" />
                    </div>
                }
            </div>
        </div>
    </div>

}

else
{
    <link rel="stylesheet" href="~/Content/css/AccesoDenegado.css">
    <div id="header">
        <h1> Acceso denegado. Esta vista solo está disponible para Administrador</h1>
        <br />
        <a href="@Url.Action("Login", "Account")" class="btn-acceso">Iniciar Sesión</a>
    </div>
}
